import styled from "styled-components";

const StyledCategory = styled.div`
  display: inline-block;
  padding: 0 16px;
  border-radius: 8px;
  background-color: var(--n40);
  text-align: center;
`;

const StyledCategoryLabel = styled.p`
  color: var(--n900);
  margin: 0;
`;

export { StyledCategory, StyledCategoryLabel };
