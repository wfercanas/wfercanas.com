import styled from "@emotion/styled";

const StyledFigure = styled.figure`
  max-width: 600px;
  margin: 64px auto;

  & img {
    width: 100%;
    object-fit: contain;
  }
`;

export { StyledFigure };
